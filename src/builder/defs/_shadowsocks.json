{
  "type": "shadowsocks",
  "server": { "$ref": "proxy:host" },
  "server_port": { "$ref": "proxy:port" },
  "method": { "$ref": "vars:/method" },
  "password": { "$ref": "vars:/password" },
  "network": "tcp",
  "multiplex": {
    "enabled": true,
    "padding": true
  }
}
