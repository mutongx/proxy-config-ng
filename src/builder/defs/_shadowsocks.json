{
  "type": "shadowsocks",
  "server": { "$ref": "args:/server" },
  "server_port": { "$ref": "args:/server_port" },
  "method": { "$ref": "args:/method" },
  "password": { "$ref": "secrets:shadowsocks_password" },
  "network": "tcp",
  "multiplex": {
    "enabled": true,
    "padding": true
  }
}