{
  "name": "proxy-config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild ./src/index.ts --bundle --outdir=dist --platform=node --format=esm"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "json-pointer": "^0.6.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230404.0",
    "@types/js-yaml": "^4.0.5",
    "@types/json-pointer": "^1.0.31",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.1"
  }
}
